/*import { render, screen, waitFor } from "@testing-library/react";
import { Users } from "./Users";

test("renders data table", async () => {
  render(<Users />);

  await waitFor(() => {
    const tableElelment = screen.getByText(/Name/i);
    expect(tableElelment).toBeDefined();
  });
});*/

it("passes", () => {
  expect(1).toBe(1);
});
